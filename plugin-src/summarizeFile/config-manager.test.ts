describe("ConfigManager", () => {
    it('loads apiKey form the config if the config exists',()=>{
        //TODO
    });
    it('asks for an api key and saves it if the config does not exist',()=>{
        //TODO
    });
});